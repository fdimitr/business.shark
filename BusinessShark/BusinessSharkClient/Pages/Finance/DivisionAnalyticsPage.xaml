<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BusinessSharkClient.Pages.Finance.DivisionAnalyticsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
    Title="Division Analytics">

    <ContentPage.Resources>
        <Style x:Key="SectionTitle" TargetType="Label">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="TextColor" Value="#222222" />
        </Style>
        <Style x:Key="SubText" TargetType="Label">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="TextColor" Value="#666666" />
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">

            <!-- Header -->
            <HorizontalStackLayout HorizontalOptions="FillAndExpand" VerticalOptions="Center" Spacing="12">
                <Label Text="Division analytics" Style="{StaticResource SectionTitle}" VerticalOptions="Center" />
                <Label Text="(30 days)" Style="{StaticResource SubText}" VerticalOptions="Center" />
            </HorizontalStackLayout>

            <!-- Chart card -->
            <Frame HasShadow="True" CornerRadius="12" Padding="12" BorderColor="#E0E0E0" BackgroundColor="White">
                <VerticalStackLayout>
                    <Label Text="Доход / Расход / Налог / Прибыль" FontAttributes="Bold" Margin="0,0,0,6" />
                    <!-- LiveCharts CartesianChart -->
                    <lvc:CartesianChart
                        x:Name="MainChart"
                        HeightRequest="320"
                        Series="{Binding Series}"
                        XAxes="{Binding XAxes}"
                        YAxes="{Binding YAxes}"
                        Tooltip="{Binding Tooltip}"
                        Legend="{Binding Legend}"
                        Margin="0" />
                    <!-- Small hint -->
                    <Label Text="Пинч-зум и перетаскивание поддерживается. Колесо мыши — масштаб." Style="{StaticResource SubText}" Margin="0,8,0,0"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Table card -->
            <Frame HasShadow="True" CornerRadius="12" Padding="0" BorderColor="#E0E0E0" BackgroundColor="White">
                <VerticalStackLayout>
                    <Label Text="Таблица: показатели по дням" FontAttributes="Bold" Margin="12,12,12,6"/>
                    <!-- Horizontal scroll for many columns -->
                    <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Always">
                        <!-- The grid will be filled in code-behind to keep XAML clean and avoid obsolete controls -->
                        <Grid x:Name="DataGrid" Padding="12" ColumnSpacing="0" RowSpacing="0" />
                    </ScrollView>
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
